<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Name="ServerPageView"
    x:Class="MTunnel.Pages.ServerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MTunnel.Pages"
    xmlns:localComponents="using:MTunnel.Components"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DataContext="{d:DesignInstance Type=local:ServerPageViewModel}"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="8"/>

        <Grid Grid.Row="1" VerticalAlignment="Top">
            <StackPanel
                x:Name="TokenViewPanel"
                Visibility="{x:Bind ViewModel.IsClientListEmpty, Mode=OneWay}"
                Spacing="12"
                Padding="16,0">
                <StackPanel Spacing="4">
                    <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Token</TextBlock>
                    <TextBlock
                        Style="{ThemeResource BodyTextBlockStyle}" 
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                        Please share this token with clients to connect
                    </TextBlock>
                </StackPanel>

                <Grid
                    Visibility="{x:Bind ViewModel.IsTokenNotEmpty, Mode=OneWay}"
                    Background="{ThemeResource SolidBackgroundFillColorQuarternaryBrush}" 
                    CornerRadius="8"
                    Padding="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock
                        Text="{x:Bind ViewModel.Token, Mode=OneWay}"
                        x:Name="TokenTextBlock"
                        FontFamily="Consolas"
                        TextWrapping="Wrap"
                        VerticalAlignment="Top"
                        IsTextSelectionEnabled="True"
                        Margin="0,0,12,0"/>

                    <localComponents:CopyButton
                        Token="{x:Bind ViewModel.Token, Mode=OneWay}"
                        Grid.Column="1"/>
                </Grid>

                <StackPanel
                    x:Name="GeneratingTokenPanel"
                    Visibility="{x:Bind ViewModel.IsTokenEmpty, Mode=OneWay}"
                    Orientation="Horizontal"
                    Spacing="12">
                    <ProgressRing IsActive="True" Width="20" Height="20"/>
                    <TextBlock VerticalAlignment="Center">Generating token, please wait...</TextBlock>
                </StackPanel>
            </StackPanel>

            <ListView
                x:Name="ConnectedViewPanel"
                Visibility="{x:Bind ViewModel.IsClientListNotEmpty, Mode=OneWay}"
                ItemsSource="{x:Bind ViewModel.Clients}"
                HorizontalAlignment="Stretch"
                Width="Auto"
                Padding="8">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="local:ConnectedClient">
                        <StackPanel Spacing="8" Padding="8">
                            <TextBlock
                                Text="{x:Bind ID}" 
                                FontFamily="Consolas"
                                FontSize="16"
                                TextWrapping="Wrap"
                                IsTextSelectionEnabled="True"/>

                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <TextBlock
                                    Style="{ThemeResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}">
                                    From:
                                </TextBlock>

                                <TextBlock
                                    Text="{x:Bind ConnectionPath}" 
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    FontFamily="Consolas" 
                                    FontSize="12"
                                    IsTextSelectionEnabled="True"
                                    TextWrapping="Wrap"/>
                            </StackPanel>


                            <localComponents:ButtonIconText
                                x:Name="DisconnectButton"
                                IconGlyph="&#xE711;"
                                HorizontalAlignment="Stretch"
                                Click="OnDisconnectClick"
                                Tag="{x:Bind}"
                                Margin="0,12,0,12">
                                Disconnect
                            </localComponents:ButtonIconText>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <localComponents:ControlOptionsControl
            x:Name="ControlOptionsControl"
            Grid.Row="2"
            TerminateClick="Terminate"
            ForceTerminateClick="ForceTerminate"
            Token="{x:Bind ViewModel.Token, Mode=OneWay}"/>
    </Grid>
</Page>
